spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://config-service:8888
      fail-fast: true
      password: admin
      username: user
      retry:
        initial-interval: 5000
        max-attempts: 6
        max-interval: 15000
        multiplier: 1.5